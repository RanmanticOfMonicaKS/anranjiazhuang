<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
    <script>
        // 1. if else 分支语句  if  如果  else  否则  
        // 2. 语法规范
        if (条件表达式) {
            // 满足条件的语句
        } else {
            // 不满足条件执行的语句
        }

        // 3. 执行思路
        // 如果条件表达式结果为真 true  则执行 if 大括号里面的 语句 代码   结束if 不会执行else 里面的代码
        // 如果条件表达式结果为假 false  则执行 else 大括号里面的 语句 代码   
        // if 和 else 里面的语句 只能执行其中的一句 
        // else 后面没有小括号 
        // 4. 代码演示
        // if (false) {
        //     console.log('满足条件了');
        // } else {
        //     console.log('不满足条件了');
        // }
        // 5. 小案例 接收用户输入的姓名，来判断是否中奖， 如果查询不到，则返回没中奖
        //  接受用户输入的数据 
        var uName = prompt('请输入您的名字:');
        // 判断是否有这个人 有就提示中奖  没有就提示没有中奖
        if (uName === '刘德华') {
            alert('恭喜您， 中了5块钱');
        } else {
            alert('思米马赛, 没有中奖，继续加油');
        }
    </script>
</head>

<body>

</body>

</html>